**命题 5.2（可动不蕴含沿给定链必动）**〔证〕。存在命题与初始账本使其三态读数可动，却沿一条容许链始终不变。

*证明*。取命题 3.3 的核及账本 $L$，其中 $v\notin N_L$，且 $\neg v$ 没有合格证明项，故 $s_L(v)=\mathsf{open}$。对任意该核的账本 $M$，定义 $T(M)$ 为补入尚缺的 $a,v$，旧见证保留，新点分别用 $\alpha,\beta$，登记前沿删去新入账点。每条引用只有从 $a$ 到 $v$ 的可能性，故新账本闭合无环，且此全函数满足假设 2.5。在 $T(L)$ 上 $s(v)=\mathsf{frozen}$，所以 $v$ 可动。另一方面，取所有 $T_t=\mathrm{id}$，每个 $L_t=L$，所以不存在状态异于初值的时刻。∎

